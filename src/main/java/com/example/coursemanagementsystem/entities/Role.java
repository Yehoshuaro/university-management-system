package com.example.coursemanagementsystem.entities;

// Enum representing different user roles in the system
public enum Role {
    ADMIN,   // Role for administrators with full access
    STUDENT, // Role for regular students
    TEACHER  // Role for instructors or teachers
}